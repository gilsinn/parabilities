<div data-controller="insert-in-list" data-insert-in-list-position-value="afterbegin">

<%= simple_form_for([@review, @comment], remote: true) do |f| %>
  <%= f.input :comment, as: :text %>
  <%= f.submit "Submit Comment", class:"btn btn-gradient" %>
<% end %>
